extends layout

block content 
  h1= title
  p= text
  div 
    ul  
      each item in data
        li
          .img  
            img(src=`/images/cat/${item.image}`, alt=item.url, style='height: 100px')
          a(href=`/cat/${item.url}`)= item.title 
          p= item.description
        
